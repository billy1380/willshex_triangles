name: willshex_draw
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
